# 核心机器学习库
xgboost==1.7.3  # 固定稳定版本，避免兼容性问题
scikit-learn==1.3.2  # 固定版本确保兼容性
numpy==1.24.3  # 固定版本避免冲突
pandas==1.5.3  # 固定版本确保稳定性
joblib==1.3.2  # 模型持久化

# 可视化相关
shap==0.42.1  # 兼容XGBoost 1.7.x
matplotlib==3.7.2  # SHAP绘图依赖

# Web框架
streamlit==1.28.0  # 使用稳定版本

# 系统优化
threadpoolctl==3.2.0  # 只需一次，删除重复项
